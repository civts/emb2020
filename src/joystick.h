#ifndef __JOYSTICK__
#define __JOYSTICK__
//Returns the x joystick position, normalized between -100 and 100
int getXposition();

//Returns the x joystick position, normalized between -100 and 100
int getYposition();
#endif
