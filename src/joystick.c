#include "joystick.h"
#include <stdio.h>

//Returns the x joystick position, normalized between -100 and 100
int getXposition(){
    printf('One');
    return 5;
}

//Returns the x joystick position, normalized between -100 and 100
int getYposition(){
    printf('Two');
    return 2;
}

